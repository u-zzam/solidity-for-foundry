/target
**/*.rs.bk
*.pdb

# Local tooling state
.gstack/

# Node (VS Code client)
node_modules/
solidity-vscode/out/
*.vsix

# Local distribution bundle (prebuilt binary + vsix, shared by hand)
/dist-local/

# Zed client build
solidity-zed/target/

# Zed-downloaded grammar sources (fetched on extension install)
solidity-zed/grammars/
solidity-zed/extension.wasm

# Local Claude Code instructions (machine-specific paths; kept out of the public repo)
CLAUDE.md

# Internal design / analysis docs (kept local, out of the public repo)
docs/
