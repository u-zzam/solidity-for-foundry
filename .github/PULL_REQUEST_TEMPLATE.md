<!-- What does this implement or fix? Keep the PR to one logical change. -->

- [ ] `cargo test` passes and `cargo clippy --all-targets` is clean
- [ ] User-facing change has a line under `[Unreleased]` in `CHANGELOG.md`
- [ ] No version numbers bumped — releases are cut by the maintainer
